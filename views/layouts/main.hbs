<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito de Compras</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="/">Tienda</a>
            <div class="navbar-nav">
                {{#if usuario}}
                    {{#if usuario.esAdmin}}
                        <a class="nav-link" href="/productos/admin">Gestionar Productos</a>
                        <a class="nav-link" href="/carrito/compras">Compras</a>
                    {{/if}}
                    <a class="nav-link" href="/carrito">
                        Carrito ({{carrito.length}})
                    </a>
                    <a class="nav-link" href="/auth/logout">Cerrar Sesión</a>
                {{else}}
                    <a class="nav-link" href="/auth/login">Iniciar Sesión</a>
                {{/if}}
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        {{{body}}}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
